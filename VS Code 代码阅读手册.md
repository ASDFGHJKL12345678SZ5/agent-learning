# VS Code 读代码手册（小白版）

> 面向：能看懂语法、但面对一堆文件和函数不知道从哪下手的人
> 目标：把 VS Code 从"高级记事本"变成"代码 X 光机"
> 用法：不用背。**卡住的时候回来查表**。用满 5 次，就是肌肉记忆。

---

## 零、先纠正一个最要命的习惯

> ❌ **读代码不是"从第一行读到最后一行"。**

这是新手最大的误区。你打开一个 500 行的文件，从第 1 行开始读，读到第 30 行遇到一个不认识的函数调用 —— 卡住了。然后怎么办？很多人是**硬着头皮往下读**，越读越懵。

> ✅ **正确的读法是"跳来跳去"。**

遇到不认识的符号 → **跳过去看它是什么** → **跳回来继续**。如此往复。

**这个手册里所有的功能，本质都是同一件事：让你在代码里"跳"，并且能"跳回来"。**

所以第一节讲的不是跳转，是**怎么跳回来** —— 这才是让"跳来跳去"变得可行的关键。

---

## 一、⭐ 最重要的一个操作（菜单里没有！）

这是全篇最值钱的一条，**右键菜单里根本没有它**，只能靠快捷键：

| 操作 | Windows / Linux | macOS |
|---|---|---|
| **返回上一个位置**（跳回来） | `Alt` + `←` | `Ctrl` + `-` |
| 前进（再跳回去） | `Alt` + `→` | `Ctrl` + `Shift` + `-` |

（Windows 上 `Ctrl+Alt+-` 也是返回，但 `Alt+←` 好按得多。
不好使就去 `Ctrl+Shift+P` 里搜 `Go Back` 看当前绑定。[参考](https://zenn.dev/mryo0826/articles/137298d4d683dd)）

**为什么这条最重要？**

因为**没有"跳回来"，你就只敢跳一次**。跳过去看完，想回原地只能靠滚轮找 —— 找三次你就放弃跳转了，又退回"线性硬读"。

有了 `Alt+←`，你可以像浏览网页一样自由跳转 —— **VS Code 会记住你的访问历史**。

> 💡 打个比方：`F12` 是"点链接"，`Alt+←` 是"浏览器后退"。
> 缺了后退键的浏览器，你敢点链接吗？

**📌 练习**：随便打开一个文件，按 5 次 `F12` 随便跳，然后**连按 5 次 `Alt+←`**，看你是否精准回到起点。

---

## 二、右键菜单逐项讲解（你截图里那一整列）

带 ⭐ 的是**你现阶段真正会天天用的**，其余知道有这么回事就行。

### 2.1 跳转类（这一组是核心）

| 菜单项 | 快捷键 | 干嘛用的 | 什么时候用 |
|---|---|---|---|
| ⭐ **转到定义** | `F12` | 跳到这个函数/变量**在哪里被定义** | **占你 80% 的用量**。看到不认识的函数就按它 |
| **转到声明** | — | 跳到"声明"处（Python 里和定义通常一样） | 少见。C/C++ 里声明和定义分开时才用得上 |
| **转到类型定义** | — | 跳到**这个变量的类型**定义处 | 想知道 `resp` 到底是什么类时用 |
| **转到实现** | `Ctrl+F12` | 跳到**接口的实现**（抽象方法/协议的具体实现） | 面向对象代码里，父类方法有多个实现时用 |
| ⭐ **转到引用** | `Shift+F12` | 列出**哪些地方用到了它**（小窗预览） | **想知道"这个函数被谁调用了"—— 理解代码的最强手段** |
| ⭐ **快速查看** | `Alt+F12` | **不跳走**，在原地弹出定义的小窗口 | 只想瞄一眼签名，不想离开当前文件。**强烈推荐** |
| ⭐ **查找所有引用** | `Shift+Alt+F12` | 引用列表**铺满左侧边栏**（比转到引用更完整） | 要找全所有调用点时 |

#### 🔑 "转到定义" vs "快速查看"，选哪个？

- **只是想知道这个函数接收什么参数** → `Alt+F12`（快速查看），瞄一眼就 `Esc` 关掉，**人不离开**
- **想看懂这个函数内部逻辑** → `F12`（转到定义），真的跳过去

**新手最容易犯的错是：一律用 F12 跳走，然后迷路。** 大量场景其实 `Alt+F12` 就够了。

#### 🔑 还有一个"隐形"操作：`Ctrl` + 鼠标左键单击

**把鼠标移到函数名上，按住 `Ctrl`，名字会变成下划线** —— 这时候单击就等于 `F12`。
这是**最快的跳转方式**，用熟了根本不会去右键菜单。

---

### 2.2 理解结构类

| 菜单项 | 快捷键 | 干嘛用的 |
|---|---|---|
| ⭐ **显示调用层次结构** | `Shift+Alt+H` | 显示"**谁调用了它 / 它调用了谁**"的树状图 |
| **显示类型层次结构** | — | 显示类的继承关系（父类/子类） |
| **查找所有实现** | — | 列出接口的所有实现类 |

> ⭐ **"显示调用层次结构"是被严重低估的功能。**
>
> 你面对一个陌生项目，最想知道的是"**程序是从哪跑起来的、怎么一步步走到这里的**"。
> 把光标放在 `main` 或者任何一个你关心的函数上，按 `Shift+Alt+H`：
> - 往上翻 = **谁调用了它**（顺着能追到程序入口）
> - 往下翻 = **它调用了谁**
>
> 这比从头读文件快 10 倍。

### 2.3 AI 相关（GitHub Copilot，装了才有）

| 菜单项 | 干嘛用的 |
|---|---|
| 将文件添加到聊天 | 把当前文件塞进 Copilot 的对话上下文 |
| 打开内联聊天 `Ctrl+I` | 在编辑器里就地提问（不用切到侧边栏） |
| 说明 | 让 AI 解释选中的这段代码 |
| 评审 | 让 AI 审查选中的代码 |

> 💡 **对你有用的用法**：选中一段看不懂的代码 → 右键 →「说明」。
> 但记住：**AI 的解释会有错**，它说的最终还是要靠 `F12` 跳过去亲自验证。

### 2.4 重构类（改代码不闯祸）

| 菜单项 | 快捷键 | 干嘛用的 |
|---|---|---|
| ⭐ **重命名符号** | `F2` | **改一处，全项目所有引用一起改** |
| **更改所有匹配项** | `Ctrl+F2` | 选中当前词的所有"文本匹配"（不做语义判断） |
| **重构...** | `Ctrl+Shift+R` | 提取函数、提取变量等 |
| **源代码操作...** | `Ctrl+.` | 快速修复（自动导包、补全拼写等） |

> ⭐ **`F2` 重命名 = 救命的。**
> 新手改函数名靠 `Ctrl+H` 全局替换 —— **这会把字符串里、注释里、别人文件里的同名内容也一起改掉**，酿成事故。
> `F2` 只改**真正的符号引用**，安全得多。
>
> ⭐ **`Ctrl+.` 快速修复 = 治"导包报错"的神器。**
> 你写了个 `httpx` 但没 import，名字下面有红波浪线 —— **光标放上去按 `Ctrl+.`，直接选"导入 httpx"**，一行都不用自己敲。
> **这个键你应该从今天开始用。**

### 2.5 Python 运行类

| 菜单项 | 干嘛用的 |
|---|---|
| **运行 Python** | 分几种子选项：在终端运行 / 调试 / 交互窗口 |
| **在交互窗口中运行** | 把选中的代码丢进 Jupyter 式交互窗口执行 |

> ⚠️ **注意"运行 Python"和"调试 Python"是两回事。**
> 运行 = 一口气跑完。**调试 = 可以按暂停、单步走** —— 那才是理解代码的终极武器，见第五节。

### 2.6 其他（可以忽略的）

| 菜单项 | 说明 |
|---|---|
| 在编辑器中启动听写 `Ctrl+Alt+V` | 语音输入。用不上 |
| 剪切/复制/粘贴 | 就是普通的剪切复制粘贴 |
| 命令面板... `Ctrl+Shift+P` | **这个很有用**，见第六节 |
| LeetCode | LeetCode 插件的菜单，跟读代码无关 |

---

## 三、不跳走也能看东西：Peek（快速查看）家族

很多人不知道，`Alt+F12` 弹出的那个小窗口里，**还能继续操作**：

| 键 | 作用 |
|---|---|
| `Alt+F12` | 弹出定义 |
| `Esc` | 关掉小窗，回到原地 |
| `Shift+F12` | 弹出引用列表（同样不跳走） |
| `Ctrl+Enter` | **在小窗里打开那个定义**（这次真的跳过去） |

> **推荐工作流**：
> 用 `Alt+F12` 快速扫 —— 大部分时候瞄一眼就够了，按 `Esc` 走人。
> 只有真的要细看，才 `Ctrl+Enter` 跳进去。

**如果非要多文件对照，另一个办法是分屏**：

| 按键 | 作用 |
|---|---|
| `Ctrl` + `\` | 把当前文件**分成左右两栏** |
| `Ctrl` + `1` / `Ctrl` + `2` | 在左右两栏之间切换焦点 |

这样左边看调用方、右边看定义，对照着读。

---

## 四、看清"整体结构"，别一头扎进细节

**新手读代码容易犯的第二个错**：一上来就读函数体内部的每一行。

正确顺序是：**先看清有哪些函数/类，再挑一个进去看**。

### 4.1 大纲视图（Outline）—— ⭐ 强烈推荐

| 按键 | 作用 |
|---|---|
| `Ctrl+Shift+O` | **列出当前文件里所有的函数、类、变量** |

按 `Ctrl+Shift+O` 后，**直接打字筛选**，回车就跳过去。

> 这个功能相当于"**目录**"。一个 700 行的文件，按一下就能看到它其实只有 9 个函数 —— **恐惧感立刻消失**。

想让它一直显示在左边：`Ctrl+Shift+P` → 输入 `Explorer` → 找到大纲视图，或者直接把资源管理器拖到左侧边栏。

### 4.2 面包屑（Breadcrumbs）

编辑器**顶部那行灰色的字**（类似 `tool.py > stream_chat > for`）就是面包屑。

它告诉你**你现在在文件的什么位置**。点它可以快速跳到上一层。

> 如果没看到：`Ctrl+Shift+P` → 搜 `Toggle Breadcrumbs`。

### 4.3 全局符号搜索

| 按键 | 作用 |
|---|---|
| `Ctrl+T` | **在整个项目里搜函数名/类名**（不是搜文本） |

> `Ctrl+T` 和 `Ctrl+Shift+F` 的区别很关键：
> - `Ctrl+T` 搜的是 **"符号"**（函数、类、变量名）
> - `Ctrl+Shift+F` 搜的是 **"文本"**（任何出现的地方，包括注释和字符串）
>
> **想找"这个函数定义在哪" → 用 `Ctrl+T`；想找"这句话在哪出现过" → 用 `Ctrl+Shift+F`。**

### 4.4 调用层次结构（再强调一次）

把光标放在任意函数上，按 `Shift+Alt+H` —— 这是**唯一能让你看到"代码是怎么被串起来的"**的功能。

---

## 五、⭐ 终极武器：断点调试（真正"看见"代码怎么跑）

前面所有功能都是**静态看代码**。而调试是**看代码真实运行的样子** —— 它能回答静态阅读永远答不了的问题：

- 这个变量跑到这一行时，**到底是什么值**？
- 这个 `if` 走的是哪一支？
- 这个循环**转了几圈**？

### 5.1 三个核心键

| 按键 | 作用 | 白话 |
|---|---|---|
| `F9` | **打/取消断点** | 在行号左边点一下也有同样效果（出现红点） |
| `F5` | **开始调试** | 程序跑到断点会**停下来** |
| `Shift+F5` | 停止调试 | |

### 5.2 停下之后，怎么"走"

| 按键 | 名字 | 白话 |
|---|---|---|
| `F10` | 单步跳过 (Step Over) | **执行这一行，然后停在下一行**。最常用 |
| `F11` | 单步进入 (Step Into) | **钻进这一行调用的函数里面** |
| `Shift+F11` | 单步跳出 (Step Out) | 从这个函数里出来，回到调用它的地方 |
| `F5` | 继续 (Continue) | 一路跑到下一个断点 |

### 5.3 停下之后，看什么

左边会出现一个 **"变量" (Variables) 面板**，实时显示**当前所有变量的值**。

**把鼠标悬停在任何变量上，也会弹出它的当前值。**

> ⭐ **对你来说，这是理解自己代码最快的方式。**
>
> 比如你 `04_boundary.py` 里那个 `judge_int(answer, truth)` ——
> 与其盯着代码想"它到底怎么判定的"，不如在函数第一行打个 `F9`，按 `F5`，
> **然后直接看 `answer` 和 `truth` 这两个变量里装的是什么**。
>
> 一眼就懂了，不用推理。

**📌 练习**：在 `tool.py` 的 `get_completion` 里，给 `data = chat(...)` 那一行打个断点，跑一次 `03_prompt_basics.py`，看 `data` 这个 dict 展开后长什么样。

---

## 六、找东西：三级搜索

| 想找什么 | 用什么 | 快捷键 |
|---|---|---|
| 某个**文件** | 快速打开 | `Ctrl+P` |
| 某个**函数/类** | 转到符号 | `Ctrl+T` |
| 当前文件里的**某段文字** | 文件内查找 | `Ctrl+F` |
| 整个项目里的**某段文字** | 全局搜索 | `Ctrl+Shift+F` |
| 整个项目里的**替换** | 全局替换 | `Ctrl+Shift+H` |
| **任何命令**（忘了快捷键） | 命令面板 | `Ctrl+Shift+P` |

> ⭐ **`Ctrl+P` 有个隐藏技巧：**
> - 输入 `文件名` → 跳文件
> - 输入 `文件名:行号`（如 `tool.py:40`）→ **直接跳到第 40 行**
> - 输入 `#函数名` → 搜符号
>
> **`Ctrl+Shift+P`（命令面板）是终极兜底**：任何功能忘了快捷键，就在这里打中文/英文搜。
> 比如 `Ctrl+Shift+P` → 输入 `Go Back`，就能看到"返回"当前绑的是哪个键。

---

## 七、10 分钟实战：用这些键读懂你自己写的 `tool.py`

打开 `llm_learn/03_prompt_basics.py`，按顺序做一遍：

| 步骤 | 操作 | 你应该看到 |
|---|---|---|
| 1 | 光标放在 `run()` 里那个 `stream_chat` 上，按 **`Ctrl+Shift+O`** | 当前文件的所有函数列表 |
| 2 | 光标放回 `stream_chat`，按 **`Alt+F12`** | 弹出小窗，看到它在 `tool.py` 里的定义 |
| 3 | 按 **`Esc`** 关掉小窗 | 你还在原地，**没迷路** |
| 4 | 再按 **`F12`** | 真的跳到 `tool.py` 里的 `stream_chat` |
| 5 | 按 **`Shift+Alt+H`** | 左侧出现调用层次 —— **谁调用了它** |
| 6 | 在小窗里按 **`Alt+←`** | **跳回 `03_prompt_basics.py` 原来的位置** ⭐ |
| 7 | 光标放在 `httpx` 上，按 **`F12`** | 跳进第三方库 `site-packages/httpx/` |
| 8 | 按 **`Alt+←`** 回来 | 回到自己的代码 |
| 9 | 光标放在 `fact_sheet_chair` 上，按 **`Shift+F12`** | 看它被用在哪些地方 |
| 10 | 随便改个函数名，按 **`F2`**，改名后 `Ctrl+Z` 撤销 | 全项目引用一起改了 |

**做完这一遍，你就已经超过 80% 的初学者了。**

---

## 八、小白常见困惑（FAQ）

### Q1：按 `F12` 没反应 / 跳不过去？

**三个常见原因：**

1. **没装 Pylance**（Python 的语言服务器）。检查：左侧扩展面板搜 `Python`，微软官方那个包装上就带了 Pylance。
2. **光标没放在符号上**。`F12` 是"跳转光标所在的词"，得把光标**贴在那个名字上**（不用选中）。
3. **这个符号本来就跳不了**。动态语言的通病 —— 比如：
   ```python
   data["choices"][0]["message"]["content"]
   ```
   这里 `data` 就是个普通 dict，**VS Code 不知道它里面有什么**，自然也跳不了。
   同理 `resp = get_completion(x)` 之后再 `resp.xxx`，也常常跳不动。
   > 👉 **这不是你的错，是动态类型语言的固有限制。** 遇到这种就靠 `print` 和调试断点去查真实结构。

### Q2：跳过去发现是 `.pyi` 文件，是什么？

**`.pyi` = 类型存根文件（type stub）**，只有函数签名和注释，**没有函数体**。

比如 `F12` 跳到 `.venv/Lib/site-packages/httpx/_api.pyi` —— 这是**正常的**，说明你跳进了库的"说明书"，而不是"源码本体"。

> **看到 `.pyi` 不用慌，反而很有用**：它把**参数有哪些、类型是什么**列得清清楚楚。
> 想看真正的实现，可以在里面找同名的 `.py` 文件，或者去 GitHub 上搜。

### Q3：右键菜单里有些项是灰的？

因为**当前上下文不适用**。比如：
- 光标不在函数名上 → "转到定义"可能是灰的
- 当前语言没装对应扩展 → "转到实现"灰掉
- 没装 Copilot → 聊天相关的项不出现

### Q4：那些 `site-packages` 里的库代码，要读懂吗？

**不需要，也读不完。** 分层看就行：

1. **看函数签名**（参数叫什么、类型是什么）—— 够用
2. **看 docstring**（函数的第一段三引号注释）—— 很有用
3. **看 `.pyi` 存根** —— 最简洁
4. **真正需要时才看实现** —— 而且大部分时候没必要

> ⭐ **一个偷懒技巧**：光标放在函数名上按 `Ctrl+K Ctrl+I`（或右键 → 快速查看），然后鼠标**悬停**在函数名上 —— VS Code 会直接告诉你**参数列表和文档**。**90% 的情况，看这一眼就够了，根本不用跳。**

---

## 九、速查表（建议截图/贴在旁边）

### 最常用 TOP 8

| 按键 | 作用 | 记住它的理由 |
|---|---|---|
| `F12` | 转到定义 | 占 80% 用量 |
| **`Alt+←`** | **返回** | **没有它，跳转就没法用** |
| `Alt+F12` | 快速查看（不跳走） | 比 F12 更常用，不迷路 |
| `Shift+F12` | 转到引用 | 看"谁调用了它" |
| `Shift+Alt+H` | 调用层次结构 | 看清整个调用链 |
| `Ctrl+Shift+O` | 文件内符号（目录） | 消灭"文件太长"的恐惧 |
| **`Ctrl+.`** | **快速修复（自动导包）** | 治红波浪线 |
| `F2` | 重命名符号 | 安全改名 |

### 调试 TOP 5

| 按键 | 作用 |
|---|---|
| `F9` | 打断点 |
| `F5` | 开始/继续 |
| `F10` | 单步跳过 |
| `F11` | 单步进入 |
| `Shift+F5` | 停止 |

### 搜索 TOP 5

| 按键 | 作用 |
|---|---|
| `Ctrl+P` | 找文件（支持 `文件:行号`） |
| `Ctrl+T` | 找符号 |
| `Ctrl+Shift+F` | 全局搜文本 |
| `Ctrl+F` | 文件内搜 |
| `Ctrl+Shift+P` | **命令面板（忘啥搜啥）** |

---

## 十、中英对照术语表

| 中文 | English | 说明 |
|---|---|---|
| 转到定义 | Go to Definition | |
| 转到引用 | Go to References | |
| 快速查看 | Peek | 不跳走的预览 |
| 调用层次结构 | Call Hierarchy | |
| 大纲视图 | Outline | |
| 面包屑 | Breadcrumbs | 编辑器顶部那行 |
| 断点 | Breakpoint | |
| 单步跳过 / 进入 / 跳出 | Step Over / Step Into / Step Out | |
| 重命名符号 | Rename Symbol | |
| 快速修复 | Quick Fix | |
| 命令面板 | Command Palette | |
| 类型存根 | Type Stub (`.pyi`) | |
| 语言服务器 | Language Server | 提供跳转/补全的引擎 |

---

## 十一、最后一条心法

> **读代码的顺序是：先框架，后细节；先跳转，后精读；先跑起来，后想明白。**

具体动作：

1. `Ctrl+Shift+O` 看清**这个文件有几个函数**
2. `Shift+Alt+H` 看清**它们怎么串起来的**
3. 挑最关键的 1~2 个函数，`F12` 进去
4. 遇到不认识的，`Alt+F12` 瞄一眼就回来
5. 还是想不通 → **打断点，让它跑一遍给你看**

**别硬读。读不懂的代码不是靠"再看一遍"看懂的，是靠"跑一遍"看懂的。**

---

*配套练习：`llm_learn/tool.py`、`llm_learn/03_prompt_basics.py`、`llm_learn/05_stream_vs_not.py`*
*创建于 2026-09-14*
