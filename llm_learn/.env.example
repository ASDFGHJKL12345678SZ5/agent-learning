# ============================================================
# 把它复制一份、改名为 .env，然后填上你自己的值
#     copy .env.example .env
# ⚠️ .env 里是密钥，绝对不要提交到 GitHub！
#    确认 .gitignore 里有 ".env" 这一行（你的仓库里已经有了）
# ============================================================

# 1) 接口地址
#    DeepSeek 用这一个就行（带不带 /v1 通常都能用，建议带上）
LLM_BASE_URL=https://api.deepseek.com/v1

# 2) 你的 API Key
#    创建地址：https://platform.deepseek.com/api_keys
#    一般以 sk- 开头，注意不要多带空格或换行
LLM_API_KEY=sk-在这里粘贴你的key

# 3) 模型名
#    ⚠️ 别靠记忆猜！各家每隔几个月就改名
#       （DeepSeek 的旧模型名在 2026-07-24 前后被弃用过一批）
#    最可靠的办法：跑 00_list_models.py，让服务器把清单告诉你
#        命令：  python 00_list_models.py
#
#    DeepSeek 当前（2026-09 实测）可用：
#        deepseek-flash      ← 快、便宜，学习阶段用这个
#        deepseek-v4-pro     ← 更强，复杂任务用
LLM_MODEL=deepseek-flash
